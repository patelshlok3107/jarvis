NEXT_PUBLIC_API_URL=https://your-jarvis-backend.onrender.com
# Optional override — leave UNSET to use same-origin /api/download
# (resumable, no GitHub redirect stall). Only set this if you intentionally
# want to force a GitHub release URL, e.g.:
# NEXT_PUBLIC_ANDROID_APK_URL=https://github.com/patelshlok3107/jarvis/releases/download/v2.1.0/JARVIS-v2.1.0.apk
